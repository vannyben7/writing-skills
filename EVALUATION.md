# 评估范围与结果

本项目按**功能实验版**分发：`human-writing` 2.12.1 与 `academic-clarity` 1.1.1。目前没有证明整体文风优于旧版技能或直接提示。这里分别记录一般改写试验、工作流检查和已完成的修复后定向回归；不同阶段不能合并成一个总成功率。

本文仅公开汇总结果和方法限制，不包含原始运行日志、测试文本、模型输出、私人样文、真实档案或内部运行标识。结构检查、模型裁判和读者偏好是不同证据。

## 一般改写试验：R2 开发集

这一阶段使用 **Human Writing 2.12.0 + Academic Clarity 1.1.1**。Human Writing 2.12.1 的普通 `SKILL.md` 正文与 2.12.0 逐字一致，仅版本元数据不同；其行为差异位于档案参考规则，并由后文的 5 案例比较单独检验。一般改写数据不应误标为对最新档案规则的直接测试。

生成和裁判于 2026-09-17 通过 Codex CLI `0.154.0-alpha.6.2` 执行，均请求模型标识 `gpt-6-astra`、推理设置 `xhigh`，使用分开的新上下文；这不是跨独立模型的验证。裁判获得原文、任务、匿名输出和检查项，不获得技能身份。每批最多包含 4 个不同案例，不同对照与相反展示顺序分开执行。请求设置已记录，但 CLI 事件没有独立暴露实际后端设置。“直接提示”（plain）指不加载专用技能，同时保留相同任务和信息保留要求的对照。

试验包含 8 份来源文本、15 个文本与处理路径组合、45 份输出，以及 60 条有效裁判观察。每个组合比较候选版本、旧版技能与直接提示，并交换展示顺序。60 条观察不是 60 份独立文本；同一文本的不同路径和顺序也不是独立复制实验。

需要编辑的组合共有 11 个，结果按展示顺序合并如下：

| 对照 | 候选胜出 | 平局 | 顺序不稳定 | 合计 |
|---|---:|---:|---:|---:|
| 旧版技能 | 3 | 7 | 1 | 11 |
| 直接提示 | 2 | 8 | 1 | 11 |

按语言和处理路径拆分如下。单元格依次为“候选胜出 / 平局 / 顺序不稳定”，仅包含需要编辑的组合；所有路径中候选落败（loss）和未完成的组合数均为 0。

| 语言 | 路径 | 组合数 | 对旧版 | 对直接提示 |
|---|---|---:|---|---|
| 中文 | Academic Clarity | 1 | 1 / 0 / 0 | 0 / 1 / 0 |
| 英文 | Academic Clarity | 1 | 0 / 1 / 0 | 1 / 0 / 0 |
| 中文 | Human Writing | 2 | 0 / 2 / 0 | 0 / 2 / 0 |
| 英文 | Human Writing | 3 | 1 / 2 / 0 | 0 / 2 / 1 |
| 中文 | 组合路径 | 2 | 1 / 1 / 0 | 1 / 1 / 0 |
| 英文 | 组合路径 | 2 | 0 / 1 / 1 | 0 / 2 / 0 |
| 合计 | 全部路径 | 11 | 3 / 7 / 1 | 2 / 8 / 1 |

另有 4 个原本已经较好的组合：对旧版和直接提示均为 4 个平局。“顺序不稳定”表示交换展示顺序后裁判结论不一致，不能当作稳定胜出。

本次试验没有对候选输出记录忠实度、声音保留或不必要改动的问题标记。这是本次有限检查的未检出，不是普适的无误证明。多数结果为平局；少量胜出还需要考虑下面的判定争议。

### 对部分胜出的判定争议

已知标记经过只读的事后复核，但冻结的模型裁判结果与上表计数没有被改写。该复核不是新的盲测偏好证据。

- 一项中文 Academic Clarity 对旧版的胜出依赖于：是否必须继续明写一个已由数值比较、局部范围和后续实验安排承接的解释，以及重复实验仍有价值的澄清。复核认为，将这些压缩明确判为实质失真，依据不足。
- 一项英文 Academic Clarity 对直接提示的胜出依赖于：在原有差异估计、区间和全部设计限制仍保留时，把“可能具有优势”改为“结果支持优势”是否构成实质性证据升级。复核认为，该语境下可能只是更直接的证据表述，至少不宜视为无争议的实质失真。
- 同一中文材料在组合路径中对旧版的胜出，也依赖相似判定，需要同样谨慎解释。

因此不能把这些胜出全部解释为已经确立的风格优势。本实验版不继续追逐特定胜率门槛，也没有为本次发布开启保留集。

## 工作流回归：修复前

已完成的合成工作流检查为 **11 / 12 通过**。剩余 1 项为克制文风遵循失败：模型接受了用户明确授权的停用档案临时使用，也没有提出持久状态修改，但实际改写仍保留了过于宏大的感悟表述。

该项应区分两层结果：可观察的临时授权与状态控制表现通过，用户所选文风没有充分落实。输出与文件提案中的状态承诺不能证明真实磁盘上的档案状态。

## 档案专属修复与定向回归

`human-writing` 2.12.1 针对上述已知失败作了档案专属修复。行为修改仅在 `references/style-profiles.md`；其他配套改动为版本元数据和文档。它允许在用户明确要求以已有细节表达克制感受时，压缩修辞性拔高，同时保留实质性的作者判断，不采用针对某个测试短语的禁词规则。

已完成一个有目的选取的 5 案例合成集合，以回复内容和文件提案为观察对象。它包含重跑的已知中文克制风格案例，以及 4 个新增的中英文案例，涵盖修辞性拔高和实质信念、反讽应保留的保护情境。

| 版本 | 通过 | 未通过 | 总数 |
|---|---:|---:|---:|
| Human Writing 2.12.1 | 5 | 0 | 5 |
| Human Writing 2.12.0 对照 | 4 | 1 | 5 |

两组均为 5 项完成、0 项无效。

对照仍只在已知的克制风格案例中失败：它用同样宏大的生活转变概括替代原有拔高。新版本去除了该拔高，同时保留路程增加十分钟、桥边看水、尚不习惯和未来喜好不确定。两个版本均通过其余 4 个新增案例。

这支持该特定修复在本组回复中的作用，但不是总体改善率、随机样本上的优势、真实档案持久化验证或个人风格相似度验证。集合包含已知失败案例和定向保护案例，不能视作新的广泛盲测；一般改写试验的结论不因此被改写。

## 离线评估工具链检查

36 项离线工具链测试通过。这些是工具链检查，不是 36 个新增写作案例，也不构成模型文风改善的证据。公开仓库另有确定性的结构检查命令，检查范围见 [README](README.md)。

## 尚未建立的证据

- 保留集仍未暴露或用于本轮调优，暂无保留集泛化结果。
- 没有使用真实私人样文或建立真实个人档案来验证效果。
- 没有用户阅读后的偏好认可或个人风格校准结论。
- 没有真实档案持久状态的读写、停用、恢复或迁移实测。
- 没有 AI 检测器、作者身份判断、检测规避或不可区分性的效果主张。

当前汇总结果依赖这些有限文本、生成条件和裁判判断；不应外推为所有模型、语言、文体或作者的表现。

## 发布文件与评估版本

`skills/` 下的技能指令、参考资源、脚本、元数据、原始许可证和第三方声明均从对应冻结候选复制。用于本次档案回归的 `human-writing/references/style-profiles.md` SHA-256 为：

```text
3632ed1c79373f1a43832b9ef6fb268f4565d6a01bb20bbc599c817e911457fe
```

仅以下包内 README 为公开分发做了文档调整，不改变指令或规则：

| 文件 | 相对冻结候选的文档差异 |
|---|---|
| [Human Writing README](skills/human-writing/README.md) | 标明功能实验版，移除 worker 的未发布声明，将内部测试编号改为一般失败描述，补充定向回归摘要并明确未证明整体文风优势 |
| [Academic Clarity README](skills/academic-clarity/README.md) | 将分发状态改为功能实验版，移除永久性的未发布占位说法，明确未证明整体文风优势 |
| [Academic Clarity 中文 README](skills/academic-clarity/README.zh-CN.md) | 同步上述公开分发状态和证据限制 |

入口文件保留已评估版本中的候选标识，作为来源记录；根文档说明本次功能实验版的分发定位。根目录的集成文档、白名单和离线检查器单独维护。后续补充结果时应继续核对对应版本与资源，不能把其他版本的表现归给当前版本。
